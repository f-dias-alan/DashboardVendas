import { StatusEstoquePipe } from './status-estoque-pipe';

describe('StatusEstoquePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusEstoquePipe();
    expect(pipe).toBeTruthy();
  });
});
